{
  "name": "app.flask.pokedex",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "py-build",
    "dev": "py-dev",
    "lint": "py-lint",
    "test": "py-test"
  },
  "devDependencies": {
    "lib.bash.build-tools": "workspace:*"
  },
  "bin": {
    "pokedex": "main.py"
  }
}
