[tool.poetry]
name = "maze-runner"
version = "0.1.0"
description = ""
authors = ["icarus64 <ellishogan95@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.scripts]
main = "main:main"

[tool.poetry.dependencies]
python = "^3.10.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.black]
force-exclude = '/(\.venv|venv|\.env|env|node_modules|\.git|__pycache__|build|dist|\.eggs)/'
