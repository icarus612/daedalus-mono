name: Sync Repositories

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout source repository
      uses: actions/checkout@v2
      with:
        path: daedalus-mono

    - name: Build Maze Runner Mono
      run: | 
        ./daedalus-mono/libs/bash/github-actions/build-maze-runner.sh
        