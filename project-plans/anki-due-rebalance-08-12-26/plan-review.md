# anki-due-rebalance-08-12-26.plan-review

## Round 1 — 08-12-26

```
verdict: rejected
next: plan-wrong
blocking: 5
non-blocking: 9
```

### Structural checks

```
$ validate-plan.sh anki-due-rebalance-08-12-26.md
INFO: lane file-scope disjointness is not machine-checked — verify scopes in the detail blocks.
OK: plan is structurally valid (10 subphases)

$ plan-lifecycle.sh check --plans-dir <worktree>/project-plans --root <worktree>
OK: plans dir layout is conformant
```

Both pass. Everything below is content verification.

### Findings

- [blocking] **B1 — The lint acceptance criteria are unsatisfiable as written.** 1.1 requires "`ruff check .` from the package directory exits 0" and 4.2 repeats it. Verified by running the exact ruff that `py-lint` installs (`pip install -q ruff`, unpinned → **ruff 0.16.2**) against the package: **11 errors at HEAD**, 10 in the reformatted working copy — all in `build_deck.py`, `get_deck_info.py`, `mp3_filename_update.py`, the three files the plan explicitly forbids touching ("do not reformat neighbouring files to satisfy it"; "do not 'fix' unrelated files into the diff"). A builder held to this criterion must either halt or break the scope rule. Findings at HEAD: `EXE002`×3, `I001`×3, `FURB122`, `F841` (`get_deck_info.py:22` `deck_id`), `BLE001` (`get_deck_info.py:59`), `RUF010`, `F401` (`mp3_filename_update.py:1`). Reword to "no NEW ruff findings attributable to this change", with the pre-existing set captured as a baseline first.

- [blocking] **B2 — The ruff rule-set claim is wrong, and it collides with two of the plan's own hard conventions.** The Stack table says "`ruff`, default rule set, no config file" and R7 concludes it "enforces almost nothing (E4/E7/E9/F). Green lint is not evidence of quality here." Verified with `ruff check --show-settings`: no config file exists anywhere (confirmed — no `ruff.toml`, no `[tool.ruff]`, no `~/.config/ruff/`, no `RUFF_*` env), yet ruff 0.16.2's default set enables **413 rules**, including flake8-bugbear (`B*`), `BLE001` blind-except, `EXE002` executable-without-shebang, `I001` isort, and a bandit subset (`S102/S110/S112`). Two direct collisions with the plan's Conventions section:
  - `BLE001` vs. "**Anki-is-running failure mode.** Preserve `get_deck_info.py`'s broad `except Exception`" — the new file will fail lint if it copies that shape verbatim.
  - `EXE002` vs. "must be `chmod +x` — the existing three scripts are mode 755" — verified none of the three has a shebang, which is exactly why they carry `EXE002` today. `rebalance_due.py` must get a shebang, diverging from the pattern the plan says to follow.
  R7's guidance ("rely on the Phase 2 unit tests, not on lint") is inverted; the lint gate is substantially stricter than the plan assumes, and its rule set floats because `py-lint` installs ruff unpinned.

- [blocking] **B3 — The phase structure violates the plan's own R4 mitigation for `rebalance_due.py`.** R4 states builders "must create `tests/test_<module>.py` in the *same* subphase as each new module (the phase structure above already does this)". It does not. 3.1 creates `anki_tools/rebalance_due.py` with a file scope of that file alone; its test is not created until 4.1. Verified in `~/.claude/hooks/smart-test.sh` (`run_python_tests`, lines 487–491 and the summary block at 514–518): writing a non-test `.py` with no matching test file prints "Missing required test file" and `return 2` — a hard block. The candidate paths it searches include `<dir>/../tests/test_<base>.py`, i.e. exactly `libs/python/anki-tools/tests/test_rebalance_due.py`. So 3.1 will hard-block on its first write. (`due_plan.py` is correctly paired in 2.1; `__init__.py` is exempt — `base` matching `^(__init__|__main__|setup|conf|config|settings)$` sets `require_tests=false`.) Fix: move a stub/first slice of `tests/test_rebalance_due.py` into 3.1's file scope.

- [blocking] **B4 — R1's "blocking risk" rests on a false premise and would halt the run for no reason.** R1 and 1.1 say `poetry.lock` is lock-version 2.1 from Poetry 2.1.3 while "the installed Poetry is **1.8.2**, which expects 2.0", and instruct the builder to "report it and stop". Verified: the repo's own toolchain never uses the PATH poetry. `libs/bash/build-tools/py-scripts/py-install` does `python3 -m venv .venv; source .venv/bin/activate; pip install poetry; poetry install` — so the effective Poetry is the one **inside** `.venv`, which `pip list` reports as **poetry 2.4.1 / poetry-core 2.4.0**. I ran the real operation on a throwaway copy of the manifests: adding `anki = "24.06.3"` and running `.venv/bin/poetry lock` **succeeds** ("Writing lock file", exit 0), producing a lock-version 2.1 file containing `name = "anki" / version = "24.6.3"`. Separately, PATH poetry 1.8.2 accepts the *existing* lock (`poetry check --lock` → "All set!"). R1 should be downgraded to a note and 1.1 should name `.venv/bin/poetry` (or `poetry` post-activation) as the tool, not a stop-the-run condition. Related: the lock normalizes the pin to `24.6.3` (PEP 440) while `anki.buildinfo.version` is the string `'24.06.3'` — verified both, so 1.1's "prints `24.06.3`" criterion is correct as written; worth a one-line note so nobody "fixes" the lock.

- [blocking] **B5 — D5's rationale does not hold for the min pass, and the plan applies one order to both.** `move_order` (largest `ivl` first, `card_id` tiebreak) is defined once in 2.1 and consumed by both 2.2 and 2.3. Its stated rationale — "shifting a long-interval card one day earlier is proportionally the smallest disturbance" — is a *max-pass* argument, where every move is one day at a time. In 2.3 the same order makes the min pass pull the **longest-interval cards the furthest forward**: the plan's own D6 example is a card moving from day 200 to day 5, which for a high-`ivl` card is the *largest* proportional disturbance available, the exact inverse of the stated principle. D5 as written asks the user to ratify one decision on a rationale that only covers half its blast radius. Split it: max-pass selection order, and min-pass selection order (where smallest-`ivl`-first is the argument-consistent default), each with its own recommendation. This is a behaviour-changing choice and belongs at the gate, not inside the build.

- [non-blocking] **N1 — R2 is stale.** It says the package `.venv` "contains only pip and setuptools". `init-workspace` has since run; `pip list` in that venv now shows ~45 packages (poetry 2.4.1, an editable `anki-tools` install, a `anki_tools.pth`). R2's *substance* still holds and is worth keeping: `.venv/bin/python -c "import anki"` still raises `ModuleNotFoundError` (verified), so the dependency really is undeclared and 1.1 really is a prerequisite.

- [non-blocking] **N2 — 1.1's justification for `__init__.py` is factually wrong.** It says "without `__init__.py` the directory is not an importable package, so `from anki_tools.due_plan import ...` in a test would depend on namespace-package luck". Verified: `import anki_tools` **succeeds today**, resolving as a PEP 420 namespace package (`__file__ is None`, `__path__` = `_NamespacePath([.../anki_tools])`) because the editable install's `.pth` puts the package root on `sys.path`. Adding `__init__.py` is still the right call (explicit packaging, stable behaviour under other runners) — just fix the reason, since the current wording implies a failure that does not occur and could send a builder chasing a phantom.

- [non-blocking] **N3 — The collection facts in the Stack table have already drifted.** Plan: "34,742,272 bytes … a `-wal` sidecar present". Verified now: `collection.anki2` is **33,783,808** bytes, **no `-wal` sidecar exists**, and a new `backup-2026-08-12-20.10.35.colpkg` has appeared in `~/.local/share/Anki2/User 1/backups` — i.e. Anki was opened and cleanly closed since planning. The substance (large live collection, close Anki before running) holds; the numbers are point-in-time and should be marked as such rather than cited as fixed facts.

- [non-blocking] **N4 — Missing dependency edge on 3.1.** The syllabus declares `3.1 (after: 1.1)`, but 3.1's `collect_cards(col, deck_ids, start_day) -> list[CardDue]` returns the frozen dataclass defined in **2.1**. That is a real `after:` edge the syllabus does not declare. Harmless while the plan stays single-lane and executes in syllabus order, but it is exactly the edge that breaks if a dispatcher ever reorders or parallelizes. Should read `(after: 1.1, 2.1)`.

- [non-blocking] **N5 — `end_day` is used but never defined.** It is a parameter of `apply_max_pass` and `apply_min_pass`, the loop bound in both pseudocode blocks, and appears in post-conditions 3 and 4 — yet nothing states how it is derived, and `plan_rebalance`'s signature does not take it. Inferable from 2.1 ("every day in `[start_day, max_day]` gets a key") as the maximum `day` among the input cards, but a `contract-tester` writing tests blind from the contract cannot infer it. State it explicitly in 2.1.

- [non-blocking] **N6 — The `get_deck_info.py` line citations are correct against HEAD but already stale in the worktree.** `:5-11` (`get_anki_collection_path`), `:39-40` (`pdash`), `:58` (`col.close()`), `:59-61` (the except + "Make sure Anki is not running" hint) all verified exact against `git show HEAD:…`. In the worktree's *working copy* the PostToolUse formatter has reflowed the file and they are now `:6-14`, `:42-43`, `:62`, `:63-65`. Cite by symbol name as well as line, since the formatter moves them on every write.

- [non-blocking] **N7 — R4 understates the hook's blast radius, and 4.2's git-status criterion is already unsatisfiable.** Observed live this session: a `Write` to a path **entirely outside the repo** (the session scratchpad under `/tmp`) still triggered `smart-lint.sh`, which reformatted **73 files across every worktree** — including the unrelated `.workflows/lint-import-audit/` tree — and exited 2. The `anki-due-rebalance` worktree consequently carries ~30 modified files *before any build work begins*. 4.2's "`git status` in the worktree shows only files this plan's file scopes name" therefore cannot pass as worded. Reword to "restores to the pre-run baseline plus this plan's file scopes", and have the builder capture that baseline as its first action.

- [non-blocking] **N8 — Single lane is defensible, but the alternative was real.** The plan's reason (both files bind to one data contract from 2.1) is sound. Noting for the record that 3.1 is genuinely independent of 2.2–2.4 — a two-lane split (lane 1: `due_plan.py` 2.2–2.4; lane 2: `rebalance_due.py` 3.1) with a serialized 3.2 join was available once 2.1 landed, and their file scopes are disjoint (verified: no shared file between the Phase 2 and Phase 3 detail blocks except the manifests already isolated in 1.1). Raising as an option, not a required change — at this size the coordination cost plausibly exceeds the gain.

- [non-blocking] **N9 — The test hook will run tests with the wrong interpreter.** `smart-test.sh` invokes `pytest` via `command -v pytest`, which resolves to `/home/icarus64/.pyenv/shims/pytest` (**pytest 7.4.3**, global pyenv) — not the package `.venv`, where 1.1 installs the dev dependency. So hook-time test runs and `poetry run pytest` runs will use different pytest versions and different `sys.path` resolutions. Not fatal (the global interpreter is where `anki` actually lives today), but if a hook-time run fails while `poetry run pytest` is green, that is the cause, and the builder should not chase it as a code defect.

### Open questions

1. **D5 must be split before it can be answered (see B5).** What selection order should the *min* pass use? Largest-`ivl`-first (as currently specified, inherited from the max pass) systematically drags the longest-interval cards the furthest earlier; smallest-`ivl`-first is what D5's own stated rationale actually argues for. These are different features; please answer separately from the max-pass order.
2. **D2 (window start) and D3 (sink overflow)** remain the two decisions that most change observable behaviour, and both are correctly surfaced by the plan. Confirm: leave today's and all overdue cards untouched (`--start-offset 1`)? And is an uncapped pile-up on the first day acceptable when 16/day cannot absorb the load, or should the run exit non-zero instead?
3. **D6 (shift cap), D7 (write-safety default), D8 (contained pytest vs a shared `py-test` script)** are open in the plan and still need answers.
4. **B1/B2 need a decision as well as a planner fix:** with 11 pre-existing ruff errors in this package, is the gate "no new findings from this change" (recommended, and consistent with the lint-debt decision already recorded in the progress log), or should the three neighbouring files be cleaned in a separate change first?
5. Unverified and correctly flagged by the plan: **FSRS memory-state preservation across `set_due_date`** (R5). My probe confirmed `memory_state` is `None` on non-FSRS review cards, so the behaviour on FSRS-enabled cards remains untested — 4.1's assertion is the right place to settle it, and the plan's instruction to stop and escalate if it is not preserved should be kept.

### Verified facts (spot-check record)

Confirmed correct, by direct observation — these are the plan's load-bearing claims that held:

- **All five Anki 24.06.3 API claims, at the exact cited line numbers.** `decks.py:476 deck_and_child_ids`; `scheduler/base.py:203 set_due_date` with the docstring quoted verbatim; `scheduler/base.py:53 today`; `collection.py:323 create_backup`, keyword-only with the docstring verbatim; the `cards.py` type/queue/due header comments verbatim; `memory_state: FSRSMemoryState | None` at `cards.py:48`. `anki.buildinfo.version == '24.06.3'`.
- **The empirical scheduler behaviour, re-probed independently on a throwaway collection.** `deck_and_child_ids("programming::coding")` → parent + child. `find_cards('deck:"…"')` includes subdecks (2 cards) where `did:<id>` does not (1 card). `set_due_date([c], "5")` → `due = today+5`, **`ivl` unchanged at 30**, type/queue still 2. `set_due_date([c], "5!")` → `due = today+5`, `ivl` overwritten to 5. A new card given `"5"` → `type=2, queue=2, due=today+5, ivl=5`. `create_backup(backup_folder=…, force=True, wait_for_completion=True)` → `True`, `.colpkg` written. `id_for_name` is case-insensitive (`"PROGRAMMING::CODING"` resolved to the same deck id) and returns `None` for a genuinely missing deck. Every one of these matches the plan.
- **Both algorithms are internally consistent.** I hand-traced every acceptance criterion in 2.2 and 2.3 against the pseudocode: max-pass `[0,0,5]/max=2 → [1,2,2]`, `[0,0,40]/max=16 → [8,16,16]` with `sink_overflow=0`, `[0,0,60]/max=16 → [28,16,16]` with `sink_overflow=12`; min-pass `[3,20,20,20]/min=8 → [8,15,20,20]`, `[0,8,20] → [8,8,12]` (day 2 untouched, surplus preferred), `[0,8,8] → [8,8,0]`, `[8,3] → no moves`. All reproduce exactly. 2.3's claim that the min pass cannot violate `max` is sound: it fills only up to `min`, and `validate_bounds` enforces `min <= max`. Post-conditions 1–5 are mutually consistent, including under sink overflow (day `start_day` is excluded from post-condition 3) and under the tail exemption (post-condition 4 stops at the last non-empty day).
- **Repo conventions.** `argparse` only, no `click`/`typer` anywhere. `build_deck.py:33` really is `args = parser.parse_known_args()` followed by `args.cardsfile` — the latent bug, correctly scoped out. `mp3_filename_update.py` at HEAD really is 2-space indented; `get_deck_info.py` 4-space. `.vscode/settings.json` sets `autopep8`. `package.json` `bin` has exactly the three entries named. All three scripts are mode 755. No Python tests exist anywhere in the monorepo (R6 confirmed). `virtualenvs.in-project = true` in the PATH poetry config, so 1.1's `"test": "poetry run pytest"` will find `.venv` — no finding there.
- **Toolchain versions.** Python 3.11.7; `pyproject.toml` declares `python = "^3.11"` and no other dependency; `poetry.lock` is `package = []`, `lock-version = "2.1"`, generated by Poetry 2.1.3; pnpm 9.1.0 per root `packageManager`; `turbo.json` defines `build`/`test`/`lint`/`dev`, so a package-level `test` script is picked up by `turbo test`, and `lib.bash.build-tools` has a `build` script satisfying `^build`.

## Round 2 — 08-12-26

```
verdict: rejected
next: plan-wrong
blocking: 1
non-blocking: 6
```

### Structural checks

```
$ validate-plan.sh anki-due-rebalance-08-12-26.md
INFO: lane file-scope disjointness is not machine-checked — verify scopes in the detail blocks.
OK: plan is structurally valid (10 subphases)

$ plan-lifecycle.sh check --plans-dir <worktree>/project-plans --root <worktree>
OK: plans dir layout is conformant
```

Both pass. Everything below is content verification.

### Round 1 disposition

All five round-1 blockers are resolved, and I re-verified each fix against reality rather than accepting the plan's word:

- **B1 (unsatisfiable lint criteria) — fixed and the new numbers are correct.** 1.1 Step 0 now captures a baseline and 1.1/4.2 are worded as "no NEW findings". I re-ran the exact linter `py-lint` installs, against a clean `git archive HEAD` snapshot: **11 errors**, and the plan's enumeration is exact — `EXE002`×3 (`build_deck.py:1`, `get_deck_info.py:1`, `mp3_filename_update.py:1`), `I001`×3 (same three), `FURB122` (`build_deck.py:6:5`), `F841` (`get_deck_info.py:22:9`, `deck_id`), `BLE001` (`get_deck_info.py:59:12`), `RUF010` (`get_deck_info.py:60:37`), `F401` (`mp3_filename_update.py:1:8`). The claim that `ruff check .` still exits non-zero is right, and `py-lint` is `set -e` with a bare `ruff check .`, so `pnpm lint` is red for this package today and stays red — correctly anticipated.
- **B2 (ruff rule-set claim) — fixed and now accurate.** `ruff --version` → **0.16.2**; `ruff check --show-settings` `linter.rules.enabled` block spans lines 58–472 and contains **413 distinct rule codes**; no config file exists (`ruff.toml`/`.ruff.toml`/`setup.cfg`/`.flake8` — none found). The `PostToolUse` toolchain is confirmed independently: `smart-lint.sh:361-385` runs `black .` (check then reformat) and `flake8 . --max-line-length=88 --extend-ignore=E203` when no flake8 config exists; both `black` and `flake8` resolve to pyenv shims. Two toolchains, both live, exactly as the plan now says.
- **B3 (test-file hard block) — fixed correctly.** `tests/test_rebalance_due.py` is now in **3.1's** file scope with an explicit "4.1 extends, never creates". Verified against `smart-test.sh:427-450, 487-492`: `test_candidates` includes `"${dir}/../tests/test_${base}.py"`, which for `anki_tools/rebalance_due.py` is exactly `libs/python/anki-tools/tests/test_rebalance_due.py`; a miss prints "Missing required test file for: …" and `return 2`. `require_tests=false` for `^(__init__|__main__|setup|setup.py|conf|config|settings)$` confirms `__init__.py` is exempt, so 1.1 correctly needs no paired test.
- **B4 (false Poetry blocker) — fixed.** R1 is downgraded to a note and 1.1 names `.venv/bin/poetry`. Verified: `.venv/bin/poetry --version` → **2.4.1**; PATH `poetry --version` → 1.8.2. Also verified the plan's remaining PATH-poetry usage is safe: `poetry run python -c …` from the package dir succeeds under 1.8.2 and resolves `sys.executable` to the in-project `.venv/bin/python`, so `"test": "poetry run pytest"` in `package.json` works despite the lock-version 2.1 file.
- **B5 (one selection order for two passes) — fixed.** D5 is split into D5a/D5b with independent recommendations and opposed rationales, and 2.1 now defines `max_move_order` (largest `ivl` first) and `min_move_order` (smallest `ivl` first) as separate functions, with an acceptance criterion asserting they are exact reverses on a shared input. The reasoning is sound.

Non-blocking round-1 items N1–N9 are all addressed in the revision (R2 corrected, `__init__.py` rationale corrected, collection facts marked point-in-time, `3.1 (after: 1.1, 2.1)`, `end_day` defined in 2.1, cite-by-symbol convention added, 4.2's git criterion rewritten as diff-vs-base after `git restore`, the single-lane alternative recorded, and the pytest-interpreter mismatch promoted to R8). Spot-checks that held: `get_deck_info.py` at HEAD really is `get_anki_collection_path` at `:5-11`, `pdash` at `:39-40`, `col.close()` at `:58`, the `except`/hint at `:59-61`; `command -v pytest` → `/home/icarus64/.pyenv/shims/pytest`, **pytest 7.4.3**; all five Anki API citations re-confirmed at their exact lines (`decks.py:476`, `scheduler/base.py:203`, `scheduler/base.py:53`, `collection.py:323`, `errors.py:13`/`:82`); the backup folder holds `.colpkg` files; `pyproject.toml` already declares `packages = [{include = "anki_tools"}]`, so 1.1's stated goal of "making that true" is coherent.

### Findings

- [blocking] **B6 — Phase 2's data contract gives the two ordering functions no access to `ivl`, so four signatures are unresolvable.** 2.1 fixes buckets as `dict[int, list[int]]` — "day number to the list of **card ids** on it" — and in the same section declares `max_move_order(cards_on_day) -> list[int]` and `min_move_order(cards_on_day) -> list[int]`, both of which order by `ivl`. 2.2's pseudocode consumes them as "first `excess` cards of **`buckets[d]`** in `max_move_order`" and 2.3 as "first card of **`buckets[s]`** in `min_move_order`", i.e. the argument is a `list[int]` of card ids. Card ids do not carry `ivl`, and `CardDue` — the only carrier of `ivl` — reaches the module solely as `plan_rebalance`'s `cards: Sequence[CardDue]`. No conduit from that input to the ordering functions is specified anywhere: `apply_max_pass(buckets, start_day, end_day, max_per_day)` and `apply_min_pass(buckets, start_day, end_day, min_per_day)` take no `ivl` index either, yet both must invoke an `ivl`-ordering function. The two candidate readings are mutually exclusive and each is contradicted by the plan's own text: if `cards_on_day` is `Sequence[CardDue]` it contradicts "buckets are `dict[int, list[int]]`" and both pseudocode call sites; if it is `list[int]` the `ivl` ordering is uncomputable and 2.1's acceptance criterion "`max_move_order` on day 10 of that input returns `[2, 1]` (ivl 90 before ivl 5)" cannot be written. This matters more than an ordinary omission because the skill mapping assigns test authoring to `contract-tester` "working from the detail blocks above **without reading the implementation**", and 2.1 declares itself "the data contract (every later subphase and both test files bind to this exactly)" — a blind tester and a coder will pick different signatures for four functions and two test files. Same class as round-1 N5 (`end_day`), which was fixed; this one has no inferable answer at all. Fix: state the conduit explicitly in 2.1 — e.g. `plan_rebalance` builds `ivl_by_id: dict[int, int]` (or a `dict[int, CardDue]` index) and threads it through `apply_max_pass`/`apply_min_pass` into `max_move_order(card_ids, ivl_by_id)` / `min_move_order(card_ids, ivl_by_id)` — and update the two pseudocode blocks and 2.1's acceptance criteria to match whichever shape is chosen.

- [non-blocking] **N10 — 2.2's acceptance criteria assert `sink_overflow`, which `apply_max_pass` does not produce.** `apply_max_pass(...) -> None` mutates buckets in place, but two of 2.2's criteria assert `sink_overflow == 0` and `sink_overflow == 12`. `sink_overflow` is a field of `RebalanceResult`, defined in **2.4**. A 2.2 test can only compute it as `max(0, len(buckets[start_day]) - max_per_day)` — trivially derivable, so not a blocker, but state it in 2.2 so the blind tester does not go looking for a return value that is not there. (I re-traced both numbers against the pseudocode and they are arithmetically correct: `[0,0,60]/max=16` → `[28,16,16]`, `28-16=12`.)

- [non-blocking] **N11 — 4.2 measures against two baselines that Step 0 never captures.** 4.2 requires "`pnpm lint` and `pnpm test` from the repo root do not regress versus the same commands run at 1.1's Step 0 (capture before/after)". 1.1 Step 0 enumerates exactly three artifacts — `ruff-baseline.txt`, `flake8-baseline.txt`, `scope-baseline.txt` — and says "Write **all three** into the run dir". There is no `pnpm lint` / `pnpm test` capture. Either add them to Step 0 (they are worth having: `py-lint` is `set -e` around a bare `ruff check .`, so `turbo lint` is already red for this package and a builder with no "before" cannot tell pre-existing red from regression) or drop the clause from 4.2. Note also that adding `"test": "poetry run pytest"` makes `anki-tools` the **first** Python package with a `test` script — verified, the only `test` scripts in the repo are 6 Go and 5 JS packages — so `pnpm test` at root gains a new task, and its "before" is therefore not directly comparable to its "after" for this package.

- [non-blocking] **N12 — a third divergence-from-the-cited-pattern is unflagged: `col.close()` on the error path.** Conventions says "**Always `col.close()`**, including on the error path (`get_deck_info.py:58` at HEAD)", and 3.1's acceptance requires "The collection is closed on both the success and the error path". Verified at HEAD: `:58` `col.close()` sits **inside** the `try`, with the `except` at `:59-61` — so the cited example does **not** close on the error path. The plan is prescribing an improvement (a `try/finally`) while citing the file as if it already did it. The plan already calls out two deliberate divergences from these neighbours (`BLE001` → typed excepts, `EXE002` → shebang); this is a third and should be labelled the same way, or a coder copying "the shape of `get_deck_info.py`" verbatim will fail 3.1's own acceptance criterion.

- [non-blocking] **N13 — the `odid == 0` filter is dead under the chosen enumeration route, so its skip counter will always read zero.** 3.1 resolves scope via `col.decks.deck_and_child_ids(deck_id)` and then filters `card.odid == 0` ("not in a filtered deck"), and requires a skip report of the form "skipped … 0 filtered". A card that has been pulled into a filtered deck carries the *filtered* deck's id in `did` and the original deck's id in `odid`, so enumerating by `did ∈ deck_ids` never returns it — the `odid` guard can never fire and "0 filtered" will be reported even when the deck has cards sitting in a filtered deck. Keep the guard (it is correct defence if the enumeration route ever changes to `find_cards('deck:"…"')`, which the plan lists as the alternative), but either drop the filtered counter from the skip report or state plainly that filtered cards are invisible to this route, not zero. Related and worth one line: 3.1 never names *how* cards are enumerated from `deck_ids` (`col.decks.cids(did)` vs `find_cards`), which is the fact that decides this behaviour.

- [non-blocking] **N14 — the min pass's dominant effect is compaction of the whole future queue, and the plan surfaces it only obliquely.** D6 frames the long move as an edge case ("can pull a card from day 200 to day 5"), but on any evenly-spread deck it is the *normal* outcome: I traced 100 cards spread 2/day over 50 days with `--min 8 --max 16` — the max pass makes no moves, then the min pass fills day 1 from days 2-5, day 2 from days 6-9, and so on, ending with ~12 days of exactly 8 cards and a short tail. Every card in the deck moves earlier, most of them by weeks. That is a faithful reading of rule 4 ("no day fewer than min", fill from later dates) and the plan's own invariant, so it is not a defect — but it is the single most surprising consequence of `--min 8`, it is what makes D6 a real decision rather than a nicety, and open question 2 currently asks only about *lumpiness*. Say it explicitly next to D6 so the user answers with it in view.

- [non-blocking] **N15 — `build_buckets`'s contract is silent on out-of-range input.** 2.1 says it clamps and drops nothing and that callers must pre-filter to `day >= start_day`, while also promising "every day in `[start_day, end_day]` gets a key". Those two are consistent only for conforming input; for a card with `day < start_day` the behaviour (extra key below `start_day`? `ValueError`?) is unspecified. 3.1 does filter, so this never fires in production — but a blind tester will reasonably write a case for it. One sentence resolves it.

### Open questions

1. **B6 needs no user input — it is a planner fix.** Everything below does.
2. **D5b is still unanswered** (correctly split out at round 1). The plan now recommends **smallest `ivl` first** for the min pass, with a rationale I checked and agree with: `max_move_order`'s largest-`ivl`-first rule is justified by every max-pass move being exactly one day, which is not true of the min pass. Confirm, or pick the alternative.
3. **D2 (window start) and D3 (sink overflow)** remain the two decisions that most change observable behaviour. Confirm `--start-offset 1` (today's and all overdue cards untouched)? And is an uncapped pile-up on the first day acceptable, or should the run exit non-zero instead?
4. **D6 (shift cap)** should be answered with N14 in view: with `--min 8` the tool compacts the entire future queue toward today, not just the lumpy days. Is that the intent, or is a `--max-shift` cap wanted?
5. **D7 (write-safety default)** — backup + confirm-unless-`--yes` as recommended, or the more conservative dry-run-by-default with an explicit `--apply`?
6. **D8 (contained pytest vs a shared `py-test` script)** — the plan recommends the contained option; note that this makes `anki-tools` the first Python package in the monorepo with a `test` script (verified), so `turbo test` gains a new task either way.
7. **D1, D4, D9** are recommendations I verified and have no objection to: review-queue-only scope (`queue==2 and type==2 and odid==0`), non-bang `set_due_date` (probe-confirmed to leave `ivl` untouched), and the exact `anki = "24.06.3"` pin (which the lock will normalize to `24.6.3` — correct, not a bug). Confirm and they can be recorded as settled.
8. Still unverified and correctly flagged: **FSRS memory-state preservation across `set_due_date`** (R5). 4.1 is the right place to settle it, and the plan's honest-reporting instruction should be kept.

### Verified facts (round 2 spot-check record)

Direct observations this round, none taken from the plan's word or from round 1:

- **Lint gate.** `ruff 0.16.2`; 413 enabled rules with no config file anywhere; **11 findings at HEAD** in `libs/python/anki-tools`, matching the plan's enumeration rule-for-rule and file-for-file. `py-lint` is `set -e` + `pip install -q ruff` (unpinned) + bare `ruff check .`. `smart-lint.sh` runs `black` then `flake8 --max-line-length=88 --extend-ignore=E203`; both binaries present as pyenv shims.
- **Hooks.** `smart-test.sh` candidate list and the `__init__` exemption confirmed at `:427-450`; the hard block (`return 2`) at `:487-492`; the focused run is `pytest -xvs "$test_file" -k "$base"`, and `pytest` resolves to the pyenv shim at **7.4.3** — R8 is accurate.
- **Toolchain.** `.venv/bin/poetry` **2.4.1**; PATH `poetry` **1.8.2**, and `poetry run` under it resolves to the in-project `.venv/bin/python` (so the `package.json` `test` script is fine). `.venv/bin/python -c "import anki"` still raises `ModuleNotFoundError`; `import anki_tools` succeeds as a PEP 420 namespace package (`__file__` is `None`, `__path__` is a `_NamespacePath`) — R2 and the corrected 1.1 rationale both hold. Global interpreter reports `anki.buildinfo.version == '24.06.3'`.
- **Anki API, re-read from the installed package.** `decks.py:476 deck_and_child_ids(deck_id) -> list[DeckId]`; `scheduler/base.py:203 set_due_date(card_ids, days, config_key=None) -> OpChanges` with the docstring verbatim as quoted; `scheduler/base.py:53 today` returning `days_elapsed`; `collection.py:323 create_backup(*, backup_folder, force, wait_for_completion) -> bool`, keyword-only, docstring verbatim; `errors.py:13 AnkiException`, `:28 BackendError(AnkiException)`, `:82 DBError(BackendError)` — so 3.1's typed-except pair is a correct and sufficient replacement for `except Exception`.
- **Repo shape.** `pyproject.toml` declares only `python = "^3.11"` plus `packages = [{include = "anki_tools"}]`; `package.json` has exactly the three `bin` entries and scripts `install/build/lint/dev` (no `test`); all three scripts are mode 755; `pnpm-workspace.yaml` covers `libs/*/*` so this package is in the workspace; `turbo.json` defines `test` with `dependsOn: ["^build"]`; the only `test` scripts in the repo are 6 Go and 5 JS packages — no Python one, confirming R6/D8.
- **Environment.** `collection.anki2` is now **33,783,808** bytes with **no `-wal`** sidecar, and `backups/` holds `backup-2026-08-12-20.10.35.colpkg` newest — consistent with the plan's own "point-in-time only, do not cite the byte count as fixed" caveat, which is the right framing.
- **Algorithms re-traced independently.** Max pass: `[0,0,5]/max=2 → [1,2,2]`; `[0,0,40]/max=16 → [8,16,16]`, `sink_overflow=0`; `[0,0,60]/max=16 → [28,16,16]`, `sink_overflow=12`. Min pass: `[3,20,20,20]/min=8 → [8,15,20,20]`; `[0,8,20] → [8,8,12]` with day 2 untouched (surplus preferred); `[0,8,8] → [8,8,0]`; `[8,3] → no moves`. All match the plan's stated criteria exactly. I additionally checked post-conditions 3 and 4 against the early-`break` paths the plan does not give criteria for — `[0,0,0,5]/min=8 → [5,0,0,0]` and `[0,3,0,5]/min=8 → [8,0,0,0]` — and post-condition 4 holds in both because `L` is the last non-empty day. Max-then-min ordering is sound: the min pass fills only up to `min_per_day`, `validate_bounds` enforces `min <= max`, and `start_day` is never a source, so the sink is never drained and post-condition 3's exclusion of `start_day` is exactly right.

## Round 3 — 08-12-26

```
verdict: rejected
next: plan-wrong
blocking: 2
non-blocking: 6
```

### Structural checks

```
$ validate-plan.sh anki-due-rebalance-08-12-26.md
INFO: lane file-scope disjointness is not machine-checked — verify scopes in the detail blocks.
OK: plan is structurally valid (11 subphases)

$ plan-lifecycle.sh check --plans-dir <worktree>/project-plans --root <worktree>
OK: plans dir layout is conformant
```

Both pass. Everything below is content verification.

### Round 2 disposition

- **B6 (no conduit from `CardDue` to the ordering functions) — fixed, and fixed well.** 2.1 now introduces a single `RunState` carrying `buckets`, `ivl_by_id`, `origin_by_id`, `moved`, `start_day`, `end_day`, `max_shift`, and states every signature once. I cross-checked all eight declared signatures against every call site in 2.2–2.5: `max_move_order(state.buckets[d], state)`, `may_move_to(cid, d - 1, state)`, `move_card(cid, d, d - 1, state)`, `apply_max_pass(state, max_per_day)`, `apply_reverse_max_pass(state, max_per_day)`, `apply_min_pass(state, min_per_day)` — all consistent, no residual ambiguity. The `KeyError`-rather-than-default instruction and the "`False < True`, so untouched sorts first" note are exactly the kind of detail a blind `contract-tester` needs.
- **N10–N15 all addressed.** 2.2 now states the `sink_overflow` derivation formula and points at 2.5 for the field; Step 0 gained `pnpm-lint-baseline.txt` / `pnpm-test-baseline.txt` plus the not-like-for-like caveat; `col.close()` on the error path is now labelled the *third* deliberate divergence with a `try/finally` instruction; the `odid` guard is kept with the filtered counter explicitly dropped and the enumeration route named (`col.decks.cids(did, children=False)`); the compaction consequence is spelled out under "Consequences the user should keep in view"; `build_buckets` now raises `ValueError` for `day < start_day`.

Re-verified this round rather than taken on the plan's word: `anki/decks.py:395 def cids(self, did: DeckId, children: bool = False) -> list[CardId]` — the newly named enumeration route exists with exactly that default; `decks.py:147 id_for_name`, `:254 by_name`, `:476 deck_and_child_ids`; `scheduler/base.py:203 set_due_date(card_ids, days, config_key=None) -> OpChanges` with the docstring verbatim as quoted; `anki.buildinfo.version == '24.06.3'`; `.venv/bin/poetry --version` → **2.4.1** in the run's own worktree; `smart-test.sh:438-442` `test_candidates` includes `"${dir}/../tests/test_${base}.py"` with the hard block at `:488-490` and the `__init__` exemption at `:429`; the three existing scripts are mode 755; the profile's `backups/` holds `.colpkg` files, newest `backup-2026-08-12-20.10.35.colpkg`.

### Findings

- [blocking] **B7 — 2.2's one-day-rule acceptance criterion is unsatisfiable, and its discriminator points the wrong way.** The criterion reads: `max=3`, counts `[0, 3, 6]` → `[3, 3, 3]`, "so also assert that the card which ends on day 1 is one that passed *through* day 2, i.e. `state.moved` contains it and its day changed twice." Traced against the plan's own pseudocode and its own settled D6.3: at `d=3` the excess of 3 moves to day 2, so day 2 holds **3 untouched originals + 3 arrivals**. At `d=2` the excess is again 3, and `max_move_order`'s PRIMARY key is `cid in state.moved` with `False` first — so the three cards selected are precisely the three untouched day-2 originals. **No card passes through day 2; no card's day changes twice.** The assertion can never pass, and a coder handed a red test from a blind `contract-tester` will "fix" it by breaking the untouched-first rule that D6.3 settles. Worse, the discriminator is inverted: under the one-day rule the cards that end on day 1 are the *original day-2* cards and the *original day-3* cards sit on day 2; under a "nearest day with room" implementation the day-3 cards would land on day 1 and the day-2 originals would not move at all. Card identity does distinguish the two implementations — in exactly the opposite direction from what the plan asserts. Fix: assert that day 1 holds the three cards that started on day 2 and that day 2 holds the three that started on day 3, each having moved exactly once.

- [blocking] **B8 — 2.5's flagship "user's real case" acceptance criterion raises `InfeasibleRebalance` instead of satisfying the post-conditions.** The criterion is: "The user's real case — `min=8, max=16, max_shift=14` over a lumpy distribution such as counts `[0, 40, 2, 0, 25, 1]` — satisfies [post-conditions 1–5], with `before`/`after` summing to the same total." It names no `set_earlier`, and the two criteria immediately after it cover default-mode infeasibility and the `--set-earlier` rescue separately, so this one reads as the default path (`set_earlier=False`). Traced through 2.2's pseudocode with `start_day=1`, `end_day=6`: `d=6` skip (1 ≤ 16); `d=5` excess 9 → day 4 becomes 9, day 5 becomes 16; `d=4` skip (9); `d=3` skip (2); `d=2` excess 24 → **day 1 becomes 24**, day 2 becomes 16. Result `[24, 16, 2, 9, 16, 1]`, so 2.5 step 4's `over_max` is `[1]`, non-empty, and with `set_earlier` false the plan mandates **`raise InfeasibleRebalance`** — no `RebalanceResult` exists, so neither the post-conditions nor the `before`/`after` totals can be evaluated. `max_shift=14` never binds here (every move is one day). This is not a wrong trace on my part being generous to the plan: it is the plan working exactly as D3 specifies, on a distribution chosen to be realistic. Fix one of two ways — either add `set_earlier=True` to this criterion (and state the expected post-reverse-pass shape), or replace the distribution with one that is genuinely feasible earlier-only (its total must fit under `max` across days `start_day..end_day` *with the one-day cascade*, which for 6 days at `max=16` means the front cannot be back-loaded like this). Worth noting the same fact to the user: `[0, 40, 2, 0, 25, 1]` is the plan's own guess at the user's deck shape, and on that shape the very first `--min 8 --max 16` run fails by design.

- [non-blocking] **N16 — 2.5 has no dependents in the syllabus, though 3.2 and 3.3 both consume it.** The syllabus declares `3.2 (after: 2.4, 3.1)` and `3.3 (after: 3.2)`, so nothing anywhere depends on 2.5. But 3.2 reads `result.reverse_pass_used`, `result.short_days` and `result.end_day`, and 3.3 catches `InfeasibleRebalance` and calls `plan_rebalance` — all four defined in **2.5**, not 2.4. The `2.4` edge is a leftover from the round-2 layout in which `RebalanceResult` lived in 2.4; the same staleness survives inline at 2.1 ("`plan_rebalance(...) -> RebalanceResult` (see 2.4)" — should be 2.5), while 2.2's own note was correctly updated to "(2.5)". Harmless under serial single-lane execution, but the plan explicitly promises the edges "are declared accurately anyway ... so that a future dispatcher cannot reorder the work into a break", and this is the one edge that would break it. Fix: `3.2 (after: 2.5, 3.1)` and the 2.1 cross-reference.

- [non-blocking] **N17 — the min pass abandons a day as soon as the *nearest* source is cap-blocked, even when a further source is legal.** 2.4's inner loop takes `s` = the nearest non-empty day above `d`, picks the first card there that clears `may_move_to`, and on failure does `break # shift cap blocks this fill; day d stays short`. That break exits the fill loop for day `d` entirely rather than advancing to the next non-empty day. The case is reachable, not theoretical: the max pass deposits cards from far-out days onto early days one hop at a time, so day `d+1` can hold cards whose `origin_by_id` is high while day `d+2` holds cards sitting on their origin. With `max_shift=14`, a card on day 2 with origin 16 fails `may_move_to(cid, 1, …)` (`1 < 16 - 14`) while a card on day 3 with origin 3 passes — yet the pseudocode gives up at day 2 and leaves day 1 short. The outcome is a declared-legal `short_days` entry rather than a post-condition violation, which is why this is not blocking, but it is a silent, deterministic under-fill that no acceptance criterion would catch. Fix: on a cap block, advance `s` to the next non-empty day and only stop when no later day can supply a legal card.

- [non-blocking] **N18 — 4.1's end-to-end assertions are stale against settled D3 and D6.1, one permissively and one strictly.** "no day above 16 except possibly the sink day" predates D3: a *successful* run now guarantees post-condition 3 over the whole window **including `start_day`**, because the only two routes to an over-max sink already raised `InfeasibleRebalance`. As written the criterion would let a genuine over-max sink through the e2e gate. In the same sentence, "no day below 8 before the tail" is stricter than 2.4/2.5 allow — cap-induced `short_days` are legal and expected under the default `--max-shift 14`, so this clause fails whenever the fixture trips the cap. Fix: assert *no* day above `--max`, and assert every below-`--min` day appears in the run's reported `short_days` rather than asserting none exist.

- [non-blocking] **N19 — 2.5's `sink_overflow` criterion is only observable in `--set-earlier` mode.** "`sink_overflow > 0` exactly when the earlier-only max pass had nowhere earlier to put a card" — but in default mode that same condition is what raises `InfeasibleRebalance`, so no `RebalanceResult` is ever returned carrying a positive value. The field's own description already anticipates this ("retained for reporting even when the reverse pass resolved it"); the criterion should say so, or a blind tester writes the natural default-mode case and gets an exception.

- [non-blocking] **N20 — the `id_for_name` case-insensitivity claim is attributed to a docstring that does not exist.** 3.1 says "Deck-name matching in `id_for_name` is case-insensitive per its docstring". Read at `anki/decks.py:147-151`: `id_for_name` has **no docstring** — it is a bare `try: return DeckId(self.col._backend.get_deck_id_by_name(name)) except NotFoundError: return None`. The "ignoring case" docstring belongs to its sibling `by_name` (`:254`), which delegates to `id_for_name`, so the property is a reasonable inference about the backend call but is not documented on the function 3.1 names. The plan's own "Assumptions I could not verify" section states this correctly ("its sibling `by_name` is documented as …"); only 3.1's wording overstates it. Either soften 3.1 to match, or have 3.1 verify it against the temp collection — it is a two-line probe.

- [non-blocking] **N21 — 2.1 and 2.3 give contradictory instructions about `may_move_to`.** 2.1's `move_card` contract ends "Callers must have checked `may_move_to` first; `move_card` does not re-check", while 2.3 states plainly "`may_move_to` is NOT consulted here" and calls `move_card` directly. 2.3's reasoning is correct (the cap governs earlier displacement only, and the `start_day` floor cannot be crossed by a later move), so the code is right — but the shared contract in 2.1 is where a blind tester and a coder both look, and it currently forbids what 2.3 mandates. One clause in 2.1 carving out later-day moves resolves it. Two smaller companions worth folding into the same pass: `RebalanceResult.end_day` is undefined for the empty-`cards` early return in 2.5 step 2, and the Stack table's "no `[tool.black]` anywhere in the repo" has drifted — the main working tree's `libs/python/anki-tools/pyproject.toml` now carries a hook-added `[tool.black] force-exclude` block (uncommitted). HEAD and this run's worktree are both still clean of it, so nothing in the plan breaks; the claim is simply point-in-time, like the collection byte count the plan already caveats.

### Open questions

1. **Neither blocker needs user input.** B7 and B8 are both single-criterion rewrites inside subphases the user has already settled — no decision is reopened, no re-exploration is required, and no settled decision (D1–D9) is in question. This is a narrow planner revision, not a replan.
2. **B8 carries one thing the user may want to see before the build starts.** If the real `programming::coding` deck is back-loaded anything like the plan's guessed `[0, 40, 2, 0, 25, 1]`, the first `--min 8 --max 16` run **exits non-zero and writes nothing**, by design (D3). That is the settled behaviour and it is working correctly — but the user may prefer to know now that `--set-earlier` is likely to be needed on the first real invocation, rather than discovering it from the 4.2 dry-run. No plan change is implied either way; the 4.2 dry-run is exactly what answers it.
3. **N17 is a behaviour choice, not only a wording fix.** "Give up on this day the moment the nearest source is cap-blocked" and "keep looking further out for a legal source" are both defensible readings of D6.1, and they produce different `short_days`. The second fills more days and is what the D6.1 framing ("days that cannot reach `--min` under the cap stay under min") seems to intend, but the first is what the pseudocode says. Worth a one-line confirmation with the revision.

### Verified facts (round 3 spot-check record)

Direct observations this round, none taken from the plan's word or from earlier rounds:

- **Anki API, re-read from the installed 24.06.3 package.** `decks.py:395 def cids(self, did: DeckId, children: bool = False) -> list[anki.cards.CardId]` — the round-3 enumeration route exists with `children=False` as the default, so 3.1's "do not also pass `children=True`" instruction is right and the non-default is the risk it names; `decks.py:147 id_for_name -> DeckId | None`, no docstring (see N20); `decks.py:254 by_name` with `"""Get deck with NAME, ignoring case."""`; `decks.py:476 deck_and_child_ids`; `scheduler/base.py:203 set_due_date(card_ids, days, config_key=None) -> OpChanges` with the docstring quoted verbatim in the plan. `anki.buildinfo.version` → `24.06.3`.
- **Toolchain, inside this run's own worktree.** `.venv/bin/poetry --version` → **2.4.1**, confirming 1.1's "use `.venv/bin/poetry`, not the PATH 1.8.2". `ruff` is **not** on `PATH` and not importable from the pyenv interpreter — consistent with `py-lint` doing `pip install -q ruff` fresh on every run, which is exactly why the plan's differential-against-a-captured-baseline framing (rather than a fixed count of 11) is the right one; `black`, `flake8` and `pytest` are present as pyenv binaries, matching the `PostToolUse` toolchain.
- **Hooks.** `smart-test.sh:429` exempts `^(__init__|__main__|setup|setup.py|conf|config|settings)$`; `:438-442` `test_candidates` ends with `"${dir}/../tests/test_${base}.py"`, i.e. `libs/python/anki-tools/tests/test_rebalance_due.py` for `anki_tools/rebalance_due.py`; `:488-490` prints "Missing required test file for:" and registers a hard error. R4 and 3.1's paired-test-file structure are correct.
- **Repo state.** The worktree is on `feature/anki-due-rebalance` and is **clean** apart from untracked `project-plans/` — the ~30 pre-dirtied files that round 1's N7 recorded are gone, so 1.1's `scope-baseline.txt` will capture an empty baseline. That does not weaken 4.2's `git restore`-then-`diff --stat <base>...HEAD` procedure, which remains the right check. `pyproject.toml` at HEAD and in the worktree still declares only `python = "^3.11"` plus `packages = [{include = "anki_tools"}]`; the main working tree's copy has drifted (N21). `package.json` has exactly the three `bin` entries and scripts `install/build/lint/dev` with no `test`, so 1.1's additions are both still needed. All three `anki_tools/*.py` scripts are mode 755.
- **Environment.** `collection.anki2` is 33,783,808 bytes with no `-wal` sidecar; `backups/` newest is `backup-2026-08-12-20.10.35.colpkg`. Unchanged since round 2, and the plan's "point-in-time only, never cite the byte count as fixed" caveat is the correct framing.
- **Algorithms re-traced independently against the round-3 pseudocode.** Max pass: `[0,0,5]/max=2 → [1,2,2]`; `[0,0,40]/max=16 → [8,16,16]`, sink overflow 0; `[0,0,60]/max=16 → [28,16,16]`, sink overflow 12; `[0,3,6]/max=3 → [3,3,3]` (counts correct — the *identity* assertion is what fails, see B7). Reverse pass: `[28,16,16]/max=16 → [16,16,16,12]` with `end_day` 3→4 and total 60 preserved; `[40,0,0] → [16,16,8]` with no extension; `[50] → [16,16,16,2]` across days 1..4 with `end_day == 4`. I also checked the loop's exit condition on each: `d` never advances past `end_day`, because reaching `d == end_day` with nothing spilled into `d+1` breaks, and any spill raises `end_day` first — so `state.buckets[d]` is never a missing key. Min pass: `[3,20,20,20]/min=8 → [8,15,20,20]`; `[0,8,20]/min=8 → [8,8,12]` with day 2's card ids fully turned over (the round-2 surplus-hunting removal is genuinely observable, as the criterion claims); `[0,8,8]/min=8 → [8,8,0]`; `[8,3]/min=8 → no moves`; `[0,20]` with `max_shift=0` → no moves. Selection orders on `[CardDue(1,10,5), CardDue(2,10,90)]`: `max_move_order → [2,1]`, `min_move_order → [1,2]`, exact reverses; with `moved={2}`, `max_move_order → [1,2]`, which is the criterion that actually proves the untouched-first key is primary. Ordering of the passes re-checked: the min pass fills only up to `min_per_day` and `validate_bounds` enforces `min <= max`, so it cannot undo the max passes; in `--set-earlier` mode it may pull back cards the reverse pass pushed forward, which is legal under `may_move_to` (the cap measures from origin) and violates no post-condition.

## Round 4 — 08-13-26

```
verdict: ready
next: proceed
blocking: 0
non-blocking: 1
```

### Structural checks

```
$ validate-plan.sh anki-due-rebalance-08-12-26.md
INFO: lane file-scope disjointness is not machine-checked — verify scopes in the detail blocks.
OK: plan is structurally valid (11 subphases)

$ plan-lifecycle.sh check --plans-dir <worktree>/project-plans --root <worktree>
OK: plans dir layout is conformant
```

Both pass. Everything below is content verification.

### Round 3 disposition

Both round-3 blockers are fixed, and I independently hand-traced each fix against the plan's own pseudocode rather than accepting the plan's word:

- **B7 (one-day-rule criterion unsatisfiable, discriminator inverted) — fixed correctly.** 2.2's criterion now reads "assert day 1 holds exactly the three cards that started on day 2, and day 2 holds exactly the three that started on day 3, each having moved exactly once." I traced `max=3, start_day=1`, day2=3/day3=6 through the pseudocode by hand: `d=3` sheds 3 onto day 2 (day2 → 3 originals + 3 arrivals = 6); `d=2`'s excess of 3 is drawn via `max_move_order`, whose primary key is untouched-first, so it selects exactly the 3 original day-2 cards, which move to day 1. Final: day 1 = the three original day-2 cards, day 2 = the three original day-3 cards, each card moved exactly once. Matches the fixed criterion exactly.
- **B8 (flagship "user's real case" criterion raised `InfeasibleRebalance` instead of succeeding) — fixed correctly.** The criterion now specifies `set_earlier=True` explicitly. I re-traced `[0,40,2,0,25,1]`, `min=8, max=16, max_shift=14, start_day=1, end_day=6` end to end: max pass → `[24,16,2,9,16,1]` (sink_overflow=8, over_max=[1], matches); reverse pass (ascending sweep, excess pushed to `d+1`) → day1 sheds 8 to day2 (`[16,24,...]`), day2 sheds 8 to day3 (`[16,16,10,...]`), days 3-6 have no excess → final `[16,16,10,9,16,1]`, total 68 preserved, `end_day` stays 6 (nothing spilled past day 6) — matches the plan's stated result exactly. Min pass: days 1-5 all ≥ 8, day 6 hits the tail-exemption break → no moves, `short_days == []`. All five post-conditions hold on this trace.

Non-blocking round-3 items N16–N21 are all addressed in the revision and I re-verified each independently rather than trusting the plan's word: the syllabus now reads `3.2 (after: 2.5, 3.1)` (fixed, confirmed in the syllabus text); 2.4's inner search loop now walks every non-empty later day in ascending order rather than stopping at the first cap-blocked source, with a dedicated "N17 case" acceptance criterion; 4.1's e2e criteria now assert "no day above `--max` anywhere including the first day" and "every below-min day appears in `short_days`" rather than the stale wording; 2.5's `sink_overflow` criterion is now labelled "observable only with `set_earlier=True`"; 3.1 now correctly attributes the case-insensitivity docstring to `by_name`, not `id_for_name`, and requires a two-line probe to verify it; 2.1's `move_card`/`may_move_to` contract now has an explicit "who must call `may_move_to` first — scoped, not universal" clause naming 2.3 as the carve-out; the Stack table's ruff row already carries the `[tool.black]` point-in-time caveat.

### Findings

- [non-blocking] **N22 — `RebalanceResult.end_day` (and the rest of the result's fields) are left unspecified for the empty-`cards` early return.** 2.5 step 2 says "If `cards` is empty, return an empty `RebalanceResult` immediately — no passes, no exception," but does not state what `end_day` (or `before`/`after`) should be when there is no data to derive them from — `end_day` is elsewhere defined as "the maximum `day` among the input cards" (2.1), which is undefined for an empty sequence. The single acceptance criterion touching this case ("Empty input returns empty `moves` with no exception") does not probe `end_day` either, so a blind `contract-tester` has no way to pin it down and a coder could reasonably pick `start_day - 1`, `start_day`, or leave it as whatever an uninitialized derivation produces. Harmless in practice — nothing downstream calls `plan_rebalance` with an empty `cards` list in the real CLI path (3.1 only calls it when cards were actually collected) — but one sentence in 2.5 step 2 (e.g. "`end_day = start_day - 1`, `before = after = {}`") would close it for the record. This is the one item from round 3's N21 companion note that the revision did not touch; everything else in that finding was fixed.

### Open questions

None. All settled decisions (D1–D9) remain closed and nothing in this round reopens them. N22 is a one-line planner clarification, not a decision requiring the user's input.

### Verified facts (round 4 spot-check record)

Direct observations this round, all freshly re-run rather than taken from earlier rounds' word:

- **Ruff baseline, re-run fresh.** Installed ruff via `pip install -q ruff` exactly as `py-lint` does → **0.16.2** (unchanged since round 1). `ruff check --output-format=concise .` in `libs/python/anki-tools` → **exactly 11 findings**, identical file-for-file and rule-for-rule to the plan's enumeration: `EXE002`×3 (`build_deck.py`, `get_deck_info.py`, `mp3_filename_update.py`), `I001`×3 (same three), `FURB122` (`build_deck.py:6:5`), `F841` (`get_deck_info.py:22:9`), `BLE001` (`get_deck_info.py:59:12`), `RUF010` (`get_deck_info.py:60:37`), `F401` (`mp3_filename_update.py:1:8`).
- **Anki API, re-read directly from the installed 24.06.3 source** (not imported, to avoid an unrelated circular-import issue when importing `anki.decks` standalone in this shell): `decks.py:476 deck_and_child_ids(deck_id) -> list[DeckId]`; `decks.py:395 cids(self, did, children=False) -> list[CardId]`; `scheduler/base.py:203 set_due_date(card_ids, days, config_key=None) -> OpChanges`; `collection.py:323 create_backup(*, backup_folder, force, wait_for_completion) -> bool`; `errors.py:13 AnkiException`, `:82 DBError(BackendError)`; `cards.py` header comment "Type: 0=new, 1=learning, 2=due" / "Due is used differently..." verbatim; `cards.py:48 memory_state: FSRSMemoryState | None`. All at the exact cited lines.
- **Hooks, re-read directly.** `smart-test.sh:429` exempt regex `^(__init__|__main__|setup|setup.py|conf|config|settings)$`; `:438-442` `test_candidates` includes `"${dir}/../tests/test_${base}.py"`; `:488-491` hard block "Missing required test file for:" + `return 2`. `smart-lint.sh:361-385` runs `black . --check`/reformat then `flake8 . --max-line-length=88 --extend-ignore=E203` when no flake8 config exists. All line numbers unchanged since round 1.
- **Worktree and toolchain state.** `git status --porcelain` in the worktree shows only untracked `project-plans/` (clean otherwise), on branch `feature/anki-due-rebalance`. `pyproject.toml` still declares only `python = "^3.11"` + `packages = [{include = "anki_tools"}]`; `package.json` still has exactly the three `bin` entries and `install/build/lint/dev` scripts, no `test`. `.venv/bin/poetry --version` → **2.4.1** (found at `libs/python/anki-tools/.venv`, four directories deep — my first `find` at `-maxdepth 3` from the worktree root missed it; corrected). `.venv/bin/python -c "import anki"` still raises `ModuleNotFoundError`; `import anki_tools` still succeeds as a PEP 420 namespace package (`__file__ is None`, `_NamespacePath`). All three existing scripts confirmed mode 755. `get_deck_info.py` in the worktree is still at HEAD's exact line numbers (`:5-11`, `:39-40`, `:58`, `:59-61`) — no formatter pass has touched it yet in this worktree.
- **Repo-wide test-script census, redone with a deeper glob than earlier rounds used.** `find libs apps -maxdepth 4 -name package.json -exec grep -l '"test"' {} \;` → exactly 6 Go packages (`crud-server`, `maze-runner`, `pythonify`, `complex-dsa`, `process-monitor`, `err`) and 5 JS packages (`react/quote-builder`, `react/markdown-builder`, `react/maze-runner`, `node/maze-runner`, `node/web-crawlers`) — 11 total, none Python. No `test_*.py` or `conftest.py` anywhere in the real project tree (excluding vendored `.venv`/`node_modules` contents). Confirms R6/D8 exactly as claimed.
- **Environment.** `collection.anki2` unchanged at 33,783,808 bytes, no `-wal`; `backups/` newest still `backup-2026-08-12-20.10.35.colpkg`. `turbo.json`'s `test` task still has `dependsOn: ["^build"]`; `pnpm-workspace.yaml` still covers `libs/*/*`.
- **Algorithms, independently re-traced by hand against the round-4 pseudocode** (see Round 3 disposition above for the two flagship cases — B7's `[0,3,6]/max=3` and B8's `[0,40,2,0,25,1]` full three-pass run). Both reproduce exactly what the plan now claims.

## Round 5 — 08-14-26

```
verdict: rejected
next: plan-wrong
blocking: 2
non-blocking: 2
```

### Scope of this round

Phase 6 amendment only (subphases 6.1–6.6), per the caller's instruction. Phases 1–5 are built, shipped on PR #12, and already gated through round 4 (ready/proceed) — not re-litigated here except where Phase 6 text explicitly touches them (the `[SUPERSEDED by 6.1]` annotation on 3.2, checked below against the actual shipped code).

### Structural checks

```
$ validate-plan.sh anki-due-rebalance-08-12-26/plan.md
INFO: lane file-scope disjointness is not machine-checked — verify scopes in the detail blocks.
OK: plan is structurally valid (19 subphases)

$ plan-lifecycle.sh check anki-due-rebalance-08-12-26
OK: plan dir conformant: .../project-plans/anki-due-rebalance-08-12-26
```

Both pass. Everything below is content verification, scoped to Phase 6.

### Findings

- [blocking] **B25 — 6.1's `check_feasibility` precheck uses the SOFT sliding target as the HARD failure threshold, contradicting 6.3/6.4/DP-F's own design.** 6.1 defines the precheck's `capacity` (used by both the global upper-bound check, point 1, and the prefix/Hall condition, point 3) as `sum(T(d))` — the per-day soft target line — "in sliding mode," not `D × max_per_day` (the hard cap). This check is wired as DP-B's precheck: "Hard-fail BOTH sides, no escape flag" (settled decisions table), running "before any move planning" and exiting non-zero. But 6.3 is explicit that `T(d)` is a *soft* bound ("Days left above `T(d)` are collected into `over_target_days`. Reported, never raised" — table at line 1013, and the post-conditions at 1023-1026 repeat "Neither is asserted"), and 6.5/6.4 state outright: "**A sliding run that is merely cap-unreachable proceeds** and reports `over_target_days`" (line 1127). DP-F's entire discussion (the round's own "headline question") is built on the premise that a deck whose sliding shape can't be reached still completes as a best-effort run (option 1, recommended) rather than failing — but if 6.1's precheck already hard-fails on `total > sum(T(d))` before planning even starts, DP-F's option 1 is unreachable for exactly the decks it's meant to serve (the user's own real deck, per the 6.4 finding, needs `--max-shift ≥ 48` just to make the *shape* reachable — its `total` plausibly exceeds `sum(T(d))` at the default `--max-shift 14` even though it comfortably fits under `D × max_per_day`, the hard cap). I verified this is not a one-off phrasing slip: the prefix/Hall condition (point 3) also substitutes `target(d)` for `max_per_day` generically, so both of 6.1's precheck legs inherit the same issue in sliding mode, and 6.1 supplies no acceptance criterion that exercises a sliding-mode precheck at all (its only worked examples — `[0,40,2,0,25,1]` and its `set_earlier=True` variant — are flat-mode). 6.6's end-to-end criteria only test the "proceeds" case, never the precheck-hard-fails-on-`T(d)`-capacity case, so the contradiction was never surfaced by the plan's own test matrix. **Needs reconciliation before build**: the DP-B hard precheck almost certainly should use `max_per_day` (the constant hard cap) as `capacity` in *both* modes; `sum(T(d))` (and the cap-aware Hall check from 6.4) belong to the informational/reporting side — `anki-due-stats`'s "required vs cap-achievable profile" and `over_target_days` — not the blocking DP-B gate. If the user actually wants sliding-shape-unreachability to be a hard precheck failure too, that is a new decision point, not something 6.1 can silently assert while DP-F simultaneously assumes the opposite.

- [blocking] **B26 — 6.3 never pins how `apply_max_pass` / `apply_reverse_max_pass` / `apply_min_pass` actually use the target line `T(d)`, and the two stated acceptance criteria are only jointly satisfiable by a specific two-stage mechanism the plan doesn't specify.** The three pass signatures are still given at lines 239-241 exactly as Phase 2 shipped them (`apply_max_pass(state: RunState, max_per_day: int) -> None`, scalar `max_per_day`/`min_per_day`, no target-line parameter) — I confirmed via `grep` that no updated signature, pseudocode block, or new function appears anywhere in 6.1-6.6 showing how `T(d)` reaches the sweep. Yet 6.3's acceptance criteria require both: "a sliding run over a cap-reachable distribution leaves every day at or under `T(d)`" (the shed target is `T(d)`, not `max_per_day`) *and* "a cap-blocked sliding run does NOT raise: days stay above `T(d)` ... and remain at or under `max_per_day`" (the hard cap is still enforced even when `T(d)` isn't reachable). Naively swapping `max_per_day` for `T(d)` inside `apply_max_pass`'s existing `excess = len(bucket) - max_per_day` formula breaks the second criterion the moment the shift cap blocks enough of the shed: a day that starts at 20 with `T(d)=10` but only 2 cards able to legally move ends at 18, which is *above* `max_per_day=16` — a hard-cap violation the plan claims cannot happen. The only way I can see to satisfy both criteria is a two-stage shed (first to `max_per_day`, exactly as today, guaranteeing the unchanged feasibility/raise semantics 6.3 says stay "as today"; then a second, best-effort refinement toward `T(d)` using the same primitives, capped by the shift cap, with shortfall going to `over_target_days`) — but this isn't stated anywhere, and it arguably conflicts with "no pass acquires a `sliding` branch, and `due_plan.py` is never forked" if read as forbidding a second pass. Every other pass in this plan (2.2, 2.3, 2.4) got literal pinned pseudocode specifically because "a blind test author must reproduce it" (2.1's own stated bar); 6.3's central new mechanism does not meet that bar, and a `contract-tester` working from the detail block alone (per the skill-mapping table) has no unambiguous spec to write deterministic tests against, nor does a `coder` have one to implement against. This blocks 6.4 and 6.5 too, since both build on whatever `apply_max_pass`/`apply_min_pass` actually do with `T(d)`.

- [non-blocking] **N23 — 6.4's "~133k operations" figure overcounts by roughly 2x.** "O(D²) windows at O(1) each. For D = 365 that is ~133k operations" appears to compute `D² = 133,225` directly, but the actual number of windows `[a,b]` with `start_day <= a <= b <= end_day` is `D(D+1)/2 ≈ 66,795` — about half. Doesn't change the `O(D²)` complexity class or the "trivial, no reason to approximate" conclusion (I independently confirmed both are internally consistent with the rest of the worked table — see Verified facts below), just the concrete number quoted. A one-word fix (state it as an upper bound, or halve the figure).

- [non-blocking] **N24 — 6.6's "capture the full move set from the pre-phase code" doesn't say how the pre-phase baseline is produced.** Unlike 5.1 ("Capture the pre-change installed copy... as the comparison baseline **before** step 1 overwrites anything") or 1.1's Step-0 baseline files, 6.6 doesn't specify the mechanism for the default-mode regression check: is it a literal two-version execution (e.g., checking out the pre-Phase-6 `due_plan.py` via git and running it in a subprocess), or — more plausibly, since default-mode passes are structurally unchanged by design — is it satisfied by the *existing* Phase 2 test suite continuing to pass unchanged, plus hand-traced expected outputs for the three new distributions 6.6 calls for (the same method used throughout Phase 2's own flagship criteria, e.g. B7/B8 from earlier rounds)? Given the phase's own "Structural rule" (no pass acquires a sliding branch; defaults are the unchanged code path), I believe the latter is intended, but a blind `contract-tester` could reasonably ask where the "pre-phase" snapshot comes from. One sentence would close it.

### Open questions

None beyond what the round already scoped as user-facing (DP-F, presented at the human gate per the caller's instructions). B25 and B26 are planner-fixable — DP-F's own recommendation already states the intended behavior (best-effort + report as default); the fix is bringing 6.1's precheck and 6.3's pass mechanics in line with it, not a fresh decision for the user.

### Verified facts (round 5 spot-check record)

Direct observations this round, all freshly checked against the actual worktree rather than taken on the plan's word:

- **3.2's `[SUPERSEDED by 6.1]` annotation matches the shipped code.** Read `libs/python/anki-tools/anki_tools/rebalance_due.py` (built, on disk in the worktree) end to end: `main()`'s current order is `resolve_deck_ids → collect_cards → plan_rebalance (catching InfeasibleRebalance) → render_histogram → confirm → create_backup → apply_moves` — i.e. backup strictly *after* planning, exactly what 3.2's struck-through text describes and exactly what 6.1 proposes to change. Confirmed real, not a stale claim.
- **The absolute-day-as-offset cosmetic bug is real and precisely as described.** `main()`'s reverse-pass message reads `f"...the horizon was extended to day offset {result.end_day}."` — but `result.end_day` (from `due_plan.py`) is an **absolute** day number (verified: `plan_rebalance` sets `end_day=state.end_day`, derived from `max(card.day for card in cards)`, never offset by `today`). 6.1's fix target is accurate.
- **The "was extended" over-claim is real.** `due_plan.py`'s `plan_rebalance` sets `reverse_pass_used = True` unconditionally whenever `apply_reverse_max_pass` runs (line ~311), with no check that `state.end_day` actually grew past the pre-existing maximum. `rebalance_due.py`'s `main()` prints "the horizon was extended" whenever `result.reverse_pass_used`, with the same no-check gap. 6.1's fix target (only claim extension on a strict increase) is accurate and necessary.
- **`render_histogram`'s dead `min_per_day` parameter is real.** Read the function body: `min_per_day` is accepted but never referenced anywhere in it. Confirmed dead exactly as 6.1 claims.
- **`due_stats.py` genuinely needs `smart-test.sh` pairing.** Read `~/.claude/hooks/smart-test.sh` directly: the exempt-from-pairing regex (`^(__init__|__main__|setup|setup.py|conf|config|settings)$`) does not match `due_stats`; `anki_tools/` doesn't match the `(migrations|scripts|docs|examples)` directory exemption either; `test_candidates` includes `"${dir}/../tests/test_${base}.py"` = `libs/python/anki-tools/tests/test_due_stats.py`. 6.5's claim that the first write of `due_stats.py` hard-blocks without a paired test file in the same packet is accurate.
- **Hall's condition worked numbers independently re-derived, not just re-read.** Hand-recomputed `suggested_max` for `[0,40,2,0,25,1]`, `start_day=1`: `ceil(total/D) = ceil(68/6) = 12`; per-prefix `ceil(count(origin<=k)/(k-start+1))` for k=1..6 gives `0, 20, 14, 11, 14, 12` — max is 20 at k=2, so `suggested_max = max(12, 20) = 20`, matching the plan exactly. `avg_per_day = 68/6 = 11.33`, matching. `binding_prefix == (2, 40, 32)`: count(origin<=2) = 0+40 = 40, capacity = 2×16 = 32 — matches exactly, and confirms the plan's own point that the global average check (11.33 ≤ 16) alone would have passed while the prefix check catches it. Sparse-deck case: `floor(30/365) = 0`, matches 6.6's DP-B fixture exactly.
- **6.4's real-block table is internally self-consistent.** Re-derived independently: the block is days 244-328 at 16/day = 85 days × 16 = **1360** (exact match to the plan's figure, confirming `244..328` is the intended block, not approximate). For `max_shift=14`, worst window `a = 244-14 = 230` (matches plan); `T(d)` computed via the pinned formula (`floor(16 - 8(d-1)/364 + 0.5)`) over `[230,328]` (99 days) averages close to the plan's stated capacity of 980. For `max_shift=30`, `a = 244-30 = 214` (matches); for `max_shift=60`, `a=184`, capacity comfortably exceeds 1360 (feasible, matches). The pattern `a = block_start - max_shift` is exact and reproducible, giving me high confidence in the reported gaps (380, 204) and the "minimum feasible 48" figure as a genuine, not fabricated, computation — though I did not exhaustively re-run the bisection myself.
- **`build_target_line(1,6,8,16)` re-derived by hand**, using the pinned `floor(x+0.5)` formula: `T(1..6) = 16,14,13,11,10,8`, sum 72 — matches the plan exactly, and matches the closed form `D×(max+min)/2 = 6×24/2 = 72`.
- **No pinned target-line-aware pass signature exists anywhere in the plan.** `grep -n "target_line\|def apply_max_pass\|def apply_min_pass\|def apply_reverse_max_pass"` across the full plan file returns only the three original Phase-2 signatures (lines 239-241) and prose references to `T(d)`/`target(d)`/`over_target_days` — no updated function contract. Basis for B26.

## Round 6 — 08-14-26

```
verdict: rejected
next: plan-wrong
blocking: 2
non-blocking: 2
```

### Scope of this round

Phase 6 amendment only, per the caller's instruction: verify the round-5 blocker fixes B25 (hard-gate/soft-target separation) and B26 (two-stage shape-pass mechanism), plus non-blocking N23 (window-count arithmetic) and N24 (pre-phase baseline capture placement). Phases 1-5 are shipped and gated through round 4 — not re-litigated. DP-F remains a user decision for the human gate, not adjudicated here.

### Structural checks

```
$ validate-plan.sh anki-due-rebalance-08-12-26/plan.md
INFO: lane file-scope disjointness is not machine-checked — verify scopes in the detail blocks.
OK: plan is structurally valid (19 subphases)

$ plan-lifecycle.sh check anki-due-rebalance-08-12-26
OK: plan dir conformant: .../project-plans/anki-due-rebalance-08-12-26
```

Both pass. Everything below is content verification, scoped to Phase 6.

### Findings

- [blocking] **B25 is only half-fixed — the global-bound leg is correctly repaired, but the prefix (Hall) leg of the same hard precheck (6.1, check 3) still uses ambiguous `target(d)` notation that, per 6.4's own cross-reference, most likely still resolves to the soft `T(d)` in sliding mode — reproducing the exact defect B25 was raised against, just narrowed to one leg.** Read 6.1 (lines 871-885) closely: check 1 ("Global upper bound") is now correctly pinned to `total <= D × max_per_day` "in **BOTH** modes," with an explicit rule directly beneath it — "**The hard gate NEVER uses `T(d)`, in any mode**" — that is the real fix for the boundary case DP-F exists to serve. But that rule sentence is nested as a sub-paragraph under check 1 specifically (markdown list indentation), and check 3 ("Prefix (Hall) condition — the one that actually binds") is a separate top-level item whose formula is unchanged: `count(origin <= k) <= sum(target(d) for d in [start_day, k])`. Nothing in check 3's own text says `target(d)` means `max_per_day`; the only place `target(d)`/`target[d]` is ever defined by name in this plan is 6.3's soft sliding line `T(d)` and 6.4's windowed capacity function. And 6.4 explicitly equates the two: "**The uncapped prefix condition of 6.1 is the `a == start_day` slice of this**" (line 1139), where "this" is 6.4's `capacity(a,b) = sum(target[d] for d in [a,b])` — the same function that computes the *informational* `shape_reachable`/`min_feasible_max_shift` fields from the soft target line. If a coder follows that equivalence literally, check 3 becomes `sum(T(d) for d in [start_day, k])` in sliding mode, not `sum(max_per_day for d in [start_day, k])`. I constructed a concrete counterexample to confirm this is live, not pedantic: take `start_day=1, end_day=10, min=0, max=16` (so `T` ramps 16→0) and a prefix of 45 cards all with `origin <= 3`. Against the hard cap, `45 <= 3 × 16 = 48` — feasible. Against `sum(T(1..3)) = 16+14+12 = 42`, `45 <= 42` is **false** — the run would hard-fail on a deck that is genuinely feasible under `--max`, exactly the DP-B/DP-F contradiction B25 was written to eliminate. This matters because DP-B's own settled-decisions entry (line 1309) states plainly that "`avg > max` and `avg < min` — **and their prefix/Hall refinements** — each hard-fail... before backup and planning," so check 3 is unambiguously part of the hard gate, not an informational aside. **Fix:** rewrite check 3's formula to use `max_per_day` explicitly (e.g. `(k - start_day + 1) * max_per_day`), reserving `target(d)`/`T(d)` notation exclusively for 6.3/6.4's informational computations, and add a sliding-mode acceptance criterion for the prefix leg (mirroring the existing flat-mode `binding_prefix == (2, 40, 32)` one) that would fail if the wrong variable were substituted.

- [blocking] **B25's own fix introduces a signature the plan never pins: `check_feasibility` (6.1) has no `max_shift` parameter, yet `FeasibilityReport` fields that only 6.4 can compute (`shape_reachable`, `predicted_over_target_days`, `shape_gap`, `min_feasible_max_shift`) are mathematically dependent on `max_shift`.** 6.1's pinned signature (lines 860-868) is `check_feasibility(cards, start_day, min_per_day, max_per_day, *, sliding=False, set_earlier=False) -> FeasibilityReport` — no `max_shift`. But 6.4's condition is explicitly shift-cap-aware (`confined(a,b) = cards with origin o in [a + max_shift, b]`, line 1133) and its own acceptance criteria talk about "the check" taking `max_shift` as a parameter ("With `max_shift=None`, the check reduces to the 6.1 prefix condition," line 1156) without ever stating whether "the check" is `check_feasibility` extended with a new parameter, a second function 6.4 introduces, or something `check_feasibility` calls internally. This is precisely the ambiguity 2.1 and B26 were both held to a "blind implementer/tester must reproduce it" bar against — here a `contract-tester` writing tests for `check_feasibility`'s sliding-mode fields, or a `coder` implementing it, has to invent the missing parameter and wiring rather than follow a pinned contract. **Fix:** pin the actual signature 6.4 introduces (either `check_feasibility(..., max_shift: int | None = None, ...)` or a named helper it calls, e.g. `check_shape_reachability(cards, start_day, target, max_shift) -> ...`) and state explicitly how its output populates `FeasibilityReport`.

- [non-blocking] **N24 is documented but not actually relocated — the pre-phase capture instructions still live entirely under 6.6, with only a cross-reference asserting they belong to 6.1.** 6.6 (line 1229) says "The baseline must be captured BEFORE any Phase 6 edit — **this is the first action of 6.1**, not a step here," and the three concrete steps (copy `due_plan.py`/`rebalance_due.py` to `.artifacts/pre-phase6/`, run F1/F2/F3 through the pre-phase copy, write sorted JSON) are fully specified — but all of that text sits under 6.6's own heading (lines 1231-1233), and I re-read 6.1's "Work" section (lines 811-896) end to end: it contains no matching instruction, no reference to `.artifacts/pre-phase6/`, and no mention of F1/F2/F3. The round's stated intent was that the capture "moved to 6.1 first action" (per the caller's scope for this round); what actually happened is that 6.6 gained a pointer sentence, not that 6.1 gained the step. Low real risk — a single-lane builder reads the whole plan before starting — but it breaks the pattern this same plan sets elsewhere: 1.1's Step 0 (lint/scope baselines) and 5.1's "capture the pre-change installed copy... before step 1 overwrites anything" both state their baseline-capture instructions directly in the subphase that must perform them. **Fix:** copy (or compactly restate) 6.6's three steps directly into 6.1's "Work" section; leave 6.6 to state only the verification/comparison side.

- [non-blocking] **The 2.1/2.5 "supersession notes" the caller asked me to check are one-directional — 6.3 declares them, but 2.1 and 2.5's own text (already-built, already-shipped Phase 2 sections) carry no matching inline marker, unlike the established treatment of 3.2.** I compared directly: 3.2's ordering statement (already built) was struck through and annotated in place — `~~"plan_rebalance runs to completion before the backup is taken"~~ **[SUPERSEDED by 6.1]**` (line 582) — and round 5 verified this annotation matches the shipped code. 6.3 states "these SUPERSEDE 2.1's scalar forms" (line 1035) and "**This supersedes 2.5's orchestration sequence**... **update its step 4/5 wording accordingly**" (line 1070) — but that update was never actually performed: 2.1's pass-signature block (lines 228-251) and 2.5's "Sequence, in exactly this order" (lines 474-486) are unchanged, with no strikethrough and no `[SUPERSEDED by 6.3]` tag. This is a documentation-consistency gap only — 6.3's own text is self-contained and unambiguous about what supersedes what — but it leaves Phase 2's sections presenting now-stale signatures as current fact with no visual flag, breaking the precedent this same plan set for 3.2. **Fix:** apply the same struck-through-plus-annotation treatment to 2.1's scalar signatures and 2.5's orchestration sequence.

### Open questions

None beyond the round already scoped as user-facing (DP-F, presented at the human gate per the caller's instructions — unchanged by this round's findings, since both blocking items above are planner-fixable specification gaps, not fresh decisions for the user).

### Verified facts (round 6 spot-check record)

- **N23 is fully and correctly fixed.** 6.4 (line 1140) now states "there are exactly `D(D+1)/2` windows... For `D = 365` that is **66,795**... (An earlier draft said '~133k, O(D²)', which double-counted by treating ordered pairs as windows.)" Hand-verified: `365 × 366 / 2 = 66,795`, matches exactly. The correction is precise, not just softened language, and the surrounding complexity-class and "trivial" conclusions are unaffected, as the plan itself notes.
- **B26 is fully and correctly fixed.** Read 6.3 end to end (lines 995-1113): pass signatures are updated to take `DayTargets` mappings and explicitly marked as superseding 2.1's scalar forms; `apply_shape_pass(state, target, hard_ceiling)` is given full pinned pseudocode (lines 1074-1084) whose `if len(state.buckets[d - 1]) >= hard_ceiling[d - 1]: break` gate provably prevents the hard cap from ever being breached (the receiver is only entered when strictly under `hard_ceiling`, and the hard result was already established by the unchanged flat-cap pass in stage 1 before shaping runs); the five-step sliding sequence (hard pass → over-max resolution → shape pass → min pass → `over_target_days` collection, lines 1061-1068) is pinned and explicitly stated to supersede 2.5's orchestration; and the acceptance criteria include a directly-testable "two-stage necessity" criterion (line 1106) that requires constructing a fixture where a naive single-stage `apply_max_pass(state, T)` would overflow the sink, and asserting the specified two-stage order does not. This closes the exact gap B26 was raised against: the mechanism is no longer merely described in prose, it is pinned pseudocode plus a criterion that would fail under the wrong implementation.
- **DP-F's three-option framing is unchanged and internally consistent** with the block figures re-verified in round 5 (gap 380 at `max_shift=14`, minimum feasible 48). Presented here only as context for the caller's human-gate hand-off, not adjudicated.

## Round 7 — 08-14-26

```
verdict: ready
next: proceed
blocking: 0
non-blocking: 0
```

### Scope of this round

FINAL capped round, per the caller's instruction: verify the round-6 fixes only — B25 (hard-gate/soft-target separation, both legs), the newly-pinned `check_feasibility`/`window_violations`/`check_hard_feasibility`/`analyze_shape` signatures, N24 (pre-phase baseline capture relocated to 6.1), and the `[SUPERSEDED by 6.3]` annotations on 2.1/2.5. Phases 1-5 are shipped and gated through round 4 — not re-litigated. DP-F remains a user decision for the human gate, not adjudicated here.

### Structural checks

```
$ validate-plan.sh anki-due-rebalance-08-12-26/plan.md
INFO: lane file-scope disjointness is not machine-checked — verify scopes in the detail blocks.
OK: plan is structurally valid (19 subphases)

$ plan-lifecycle.sh check anki-due-rebalance-08-12-26
OK: plan dir conformant: .../project-plans/anki-due-rebalance-08-12-26
```

Both pass. Everything below is content verification, scoped to the round-6 fix list.

### Findings

None. All four round-6 items are fixed and hold up under independent re-derivation, not just re-reading.

- **B25, both legs, now fixed.** Check 3 (the prefix/Hall leg, line 934) now reads `window_violations(counts, start_day, end_day, capacity=constant_targets(start_day, end_day, max_per_day), max_shift)` — an explicit call to the same `constant_targets` helper the global leg uses, not the ambiguous `target(d)` notation round 6 flagged. The "hard gate never uses `T(d)`" rule is now restated directly under check 3 itself (line 936-937: "Capacity here is `max_per_day`, exactly as in checks 1 and 2 — never `T(d)`... holds for **all three checks**, not just the global one"), closing the scoping gap where the rule previously lived only as a sub-paragraph under check 1. Checks 1 and 2 are plain scalar comparisons (`total <= D × max_per_day`, `total >= D × min_per_day`) that never touch `T(d)` at all, so "all three checks use constant `max_per_day`" holds in substance for every leg even though only check 3 literally invokes the `constant_targets` function (checks 1-2 don't need the windowed kernel).
- **The never-helps proof, spot-checked directly rather than taken on the plan's word.** I derived it independently rather than trusting the "exhaustive check" claim: `T(d) = floor(x + 0.5)` where `x = max - (max-min)*(d-start)/(end-start)`. For every `d >= start`, `x <= max` (since `max >= min` and the fractional term is non-negative), so `x + 0.5 <= max + 0.5`, and `floor` of that is at most `max` (an integer). So `T(d) <= max` for **every** `d`, unconditionally — not just for `D in [2,60)` or `min < max <= 24`, but for any valid `(min, max, D)`. That single inequality is sufficient: a prefix sum of `len` terms each `<= max` is trivially `<= max * len`. The plan's exhaustive numerical check is consistent with (and a special case of) this direct proof — no gap between what was checked and what the claim needs.
- **Round-6 counterexample arithmetic — re-derived by hand, both versions are independently correct; this is a revised example, not a bug fix.** Round 6's finding used `start=1, end=10, min=0, max=16`: I recomputed `T(1)=floor(16+0.5)=16`, `T(2)=floor(16-16/9+0.5)=floor(14.722)=14`, `T(3)=floor(16-32/9+0.5)=floor(12.944)=12`, sum `42` — matches round 6's number exactly for those inputs. The current plan (line 938) uses different inputs, `min=8, max=16`: `T(1)=floor(16+0.5)=16`, `T(2)=floor(16-8/9+0.5)=floor(15.611)=15`, `T(3)=floor(16-16/9+0.5)=floor(14.722)=14`, sum `45` — matches the plan's current claim exactly. Both are correct for their own inputs; the planner switched the example's `min` from 0 to 8 (aligning it with the plan's own recurring `min=8/max=16` fixture used everywhere else), which is why the sum changed from 42 to 45. The **46-48 contradiction band is exact for the current (min=8) parameters**: at 46-48 cards, `cards <= 3*16=48` (hard-feasible) while `cards > 45` (T-infeasible) — true for 46, 47, 48; at 45 there's no contradiction (`45<=45`); at 49 the hard cap itself fails too. I also cross-checked this 46/48/45 triple against 6.4's own acceptance criterion (line 1230, "the two callers are distinguishable by test... 46 <= 48... 46 > 45") — the two subphases now cite identical, mutually consistent numbers.
- **Signatures fully pinned and cross-consistent between 6.1 and 6.4.** `window_violations(counts, start_day, end_day, capacity: DayTargets, max_shift)` (6.1, lines 878-885) is the one kernel; `check_hard_feasibility(cards, start_day, end_day, min_per_day, max_per_day, max_shift, *, set_earlier=False)` (887-897) and `analyze_shape(cards, start_day, end_day, target, max_shift)` (900-906) both take `max_shift` and `end_day` as named parameters, with `max_shift` explicitly called out as "a required parameter, not an optional extra" (line 922) — closing round 6's B25-second-half gap. `check_feasibility` (908-919) composes them: "calls `check_hard_feasibility` always, and `analyze_shape` only when `sliding` is true... `analyze_shape`'s result never influences `feasible`" (line 924) — matching the caller table in 6.4 (lines 1200-1205), which names the same two functions with the same YES/NO gating column and states plainly that wiring `T(d)` into the hard caller "is the B25 defect and is specifically forbidden." 6.4's "distinguishable by test" acceptance criterion (line 1230) exercises exactly this composition against the same 46/48/45 numbers verified above.
- **N24 — actually relocated this time, not just cross-referenced.** 6.1's "Work — (a) FIRST ACTION OF THE PHASE" (lines 815-826) now contains the full three-step capture procedure inline: copy `due_plan.py`/`rebalance_due.py` to `<parent-worktree>/.artifacts/pre-phase6/`, run the three named fixtures (F1 feasible flat, F2 reverse pass, F3 cap-blocked, all default mode) through the pre-phase copy, and write sorted-key `moves-<fixture>.json` files. 6.6 (lines 1298-1300) now only *consumes* those files and explicitly says so ("This subphase only consumes those files; if they are absent, 6.1 step (a) was skipped and the regression cannot be verified") — the inverse of round 6's finding, where 6.6 held the real instructions and 6.1 held only a pointer.
- **`[SUPERSEDED by 6.3]` annotations present on both 2.1 and 2.5, matching the 3.2 precedent style.** 2.1's pass-signature block now opens with an inline blockquote (line 227) struck-marked `[SUPERSEDED by 6.3]`, naming exactly what changes (scalar bounds → `DayTargets`, new `apply_shape_pass`) and what doesn't (`RunState`, `CardDue`, `may_move_to`, `move_card`, both selection orders). 2.5's "Sequence, in exactly this order" is preceded by the same tag (line 478), naming the three insertions (precheck, `end_day` containment, `apply_shape_pass`) and stating flat-mode step ordering is unchanged. Both read the same way 3.2's already-shipped `~~struck~~ **[SUPERSEDED by 6.1]**` annotation does, which round 5 verified against the actual shipped code.

### Open questions

None. DP-F is the one open item carried forward to the human gate, per the caller's standing instruction — unchanged by this round, since every item in this round's scope was a planner-fixable specification/consistency gap, not a fresh decision.

### Verified facts (round 7 spot-check record)

- **Structural checks, both green.** `validate-plan.sh` — 19 subphases, structurally valid. `plan-lifecycle.sh check` — plan dir conformant.
- **`grep` sweep for residual `target(d)`/`target[d]` ambiguity.** Every remaining generic `target[d]`/`target(d)` reference (lines 1137, 1151, 1197) sits inside `apply_shape_pass`'s pseudocode or 6.4's generic `capacity(a,b)` formula, both of which are explicitly parametrized functions whose caller supplies the meaning (6.4, line 1207: "`target` in the formula above is that parameter, not `T(d)`"). No occurrence of bare `target(d)`/`target[d]` remains inside the DP-B hard-gate checks (6.1's three enumerated checks) — the exact site B25 was raised against is now clean.
- **This round's four scoped items, independently re-derived rather than accepted on the plan's word:** the never-helps proof (direct inequality derivation, not just trusting the "exhaustive check" claim), the 42-vs-45 arithmetic (recomputed both parameterizations by hand), the signature cross-consistency (read 6.1 and 6.4 side by side), and the N24 relocation (confirmed the instructions physically moved, not merely referenced).
