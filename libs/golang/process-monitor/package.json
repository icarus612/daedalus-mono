{
  "name": "lib.golang.process-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o bin/process-monitor ./cmd",
    "dev": "~/go/bin/gotestsum --watch ./...",
    "test": "go test ./...",
    "lint": "go vet ./... && go fmt ./..."
  },
  "bin": {
    "process-monitor": "bin/process-monitor"
  }
}
