{
  "name": "lib.golang.pythonify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "go build ./...",
    "dev": "gotestsum --watch ./...",
    "test": "go test ./...",
    "lint": "go vet ./... && test -z \"$(gofmt -l .)\""
  }
}
