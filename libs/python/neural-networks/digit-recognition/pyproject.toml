[project]
name = "digit-recognition"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = [
  "tensorflow",
  "scikit-learn",
  "abstract-base-classes",
]

[tool.uv]
package = false

[tool.uv.sources]
abstract-base-classes = { workspace = true }
