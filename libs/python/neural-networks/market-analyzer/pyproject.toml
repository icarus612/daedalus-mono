[project]
name = "market-analyzer"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = [
  "numpy",
  "pandas",
  "tensorflow",
  "scikit-learn",
  "matplotlib",
]

[tool.uv]
package = false
