[project]
name = "abstract-base-classes"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["abstract_base_classes"]
