[project]
name = "flask-utils"
version = "0.1.0"
description = "Shared Flask app utilities (port discovery)."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["flask_utils"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
